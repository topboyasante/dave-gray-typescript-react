let myName:string
let age:number
let isLoading:boolean
let album:any
let postID : string | number  //union type
let IsActive: boolean | number //union type
let re:RegExp = /\w+/g //a regex that selects all words

const sum = (a:number,b:string) =>{
    return a+b
}